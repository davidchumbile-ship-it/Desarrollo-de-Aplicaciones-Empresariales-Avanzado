<Window x:Class="SEMANA04.Views.VentanaProveedores"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Buscar Proveedores" Height="500" Width="850"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Buscar Proveedores" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="Nombre Contacto:" VerticalAlignment="Center"/>
            <TextBox x:Name="txtNombreContacto" Width="150" Height="25" VerticalContentAlignment="Center" Margin="5,0,15,0"/>

            <Label Content="Ciudad:" VerticalAlignment="Center"/>
            <TextBox x:Name="txtCiudad" Width="150" Height="25" VerticalContentAlignment="Center" Margin="5,0,15,0"/>

            <Button x:Name="btnBuscar" Content="Buscar" Width="75" Height="25" Click="BtnBuscar_Click" Margin="0,0,5,0"/>
            <Button x:Name="btnLimpiar" Content="Limpiar" Width="75" Height="25" Click="BtnLimpiar_Click"/>
        </StackPanel>

        <DataGrid x:Name="dgProveedores" Grid.Row="2"
                  AutoGenerateColumns="True"
                  IsReadOnly="True"/>
    </Grid>
</Window>
